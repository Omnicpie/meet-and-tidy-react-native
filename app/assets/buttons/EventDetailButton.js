import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';

const Stack = createStackNavigator();

function EventDetailButton(props) {
  return (
    <div>
      <text>Boom Baby</text>
    </div>
  );
}

export default EventDetailButton;
